// import { type ThemeConfig, theme } from 'antd'
import { type ThemeConfig } from 'antd'

export interface ITheme extends ThemeConfig {}

export const themeConfig: ITheme = {
  // algorithm: theme.darkAlgorithm
}
